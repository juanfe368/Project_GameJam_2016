using UnityEngine;
using System.Collections;

public class ColisionadorEvent : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	/*void OnTriggerEnter(Collider other) {
		if (other.gameObject.name == "trapPincho1") {
			Debug.Log ("Entro a la colisión");
			//transform.Translate (0, (transform.position.y + cantidadSubir) * Time.deltaTime, 0);
		}
		else {
			Debug.Log ("Debug no colisiono");
		}
	}*/
}
